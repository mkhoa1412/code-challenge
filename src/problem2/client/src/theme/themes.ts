const lightTheme = {
    primary: '#FFF',
};

const darkTheme = {
    primary: '#000',
};
export type Theme = typeof lightTheme;

export const themes = {
    light: lightTheme,
    dark: darkTheme,
};
